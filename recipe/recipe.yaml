context:
  name: polars-st
  version: "0.5.0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://github.com/oreilles/polars-st/archive/refs/tags/${{ version }}.tar.gz
  sha256: be01129ed46fbfc1ac3f80af5955c4f8d52f0e5b93fc64112e9b71886e411fc8

build:
  number: 0
  script:
    - ${{ PYTHON }} -m pip install . --no-deps --no-build-isolation -vv
    - cargo-bundle-licenses --format yaml --output THIRDPARTY.yml

requirements:
  build:
    - ${{ compiler('rust') }}
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - ${{ stdlib('c') }}

    - if: unix
      then: make
    - if: build_platform != target_platform
      then:
        - python
        - crossenv
        - cross-python_${{ target_platform }}
    - cargo-auditable
    - python
    - puccinialin
    - maturin
    - cargo-bundle-licenses
    - cmake
    - pip
  host:
    - maturin
    - python
    - pip
  run:
    - python
    - polars >1.34
    - pyogrio >0.9.0
    - pyarrow >=19.0.1
    - certifi
  ignore_run_exports:
    from_package:
      - cross-python_${{ target_platform }}


tests:
  - python:
      imports:
        - polars_st
      pip_check: true

about:
  homepage: https://github.com/oreilles/polars-st
  summary: 'Polars ST provides spatial operations on Polars DataFrames, Series, and Expressions.'
  description: |
    polars-st provides geometry operations under the namespace st on
    Polars Expr, Series, DataFrame and LazyFrame. Functions used to read
    files or parse geometries are available at the module root.
  license: LGPL-2.1-or-later
  license_file:
    - COPYING
    - THIRDPARTY.yml
  documentation: https://oreilles.github.io/polars-st/
  repository: https://github.com/oreilles/polars-st

extra:
  recipe-maintainers:
    - hobu
